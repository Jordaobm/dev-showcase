"use client";

import { StatusApplication } from "@/features/pwa-core/components/StatusApplication";

const OfflinePage = () => {
  return (
    <main className="p-4 w-full max-w-2xl m-auto">
      <StatusApplication headingLevel="h1" />
    </main>
  );
};

export default OfflinePage;
